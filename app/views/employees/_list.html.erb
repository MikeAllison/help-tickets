<div class="row">
	<div class="col-xs-12">
		<table class="table table-striped">
			<thead>
				<th class="col-xs-4"><%= sort_column "Name", "last_name" %></th>
				<th class="col-xs-2"><%= sort_column "User Name", "last_name" %></th>
				<th class="col-xs-2"><%= sort_column "Office", "offices.name", "office" %></th>
				<th class="col-xs-2 hidden-xs">Total Tickets</th>
				<th class="col-xs-1"></th>
				<th class="col-xs-1"></th>
			</thead>
			<tbody>
				<% @employees.each do |employee| %>
					<tr>
						<td class="col-xs-4">
							<%= employee_status_icon(employee.active) %>
							&nbsp;
							<%= employee.last_first %>
						</td>
						<td class="col-xs-2"><%= employee.user_name %></td>
						<td class="col-xs-2"><%= employee.office.name %></td>
						<td class="col-xs-2 hidden-xs"><%= employee.tickets.count %></td>
						<td class="col-xs-1">
							<%= link_to edit_employee_path(employee), title: 'Edit' do %>
								<span class="glyphicon glyphicon-wrench btn btn-default btn-xs" aria-hidden="true"></span>
								<span class="sr-only" title="Edit">Edit</span>
							<% end %>
						</td>
						<td class="col-xs-1">
							<%= link_to employee, method: :delete, title: 'Delete' do %>
								<span class="glyphicon glyphicon-trash btn btn-default btn-xs" aria-hidden="true"></span>
								<span class="sr-only" title="Delete">Edit</span>
							<% end %>
						</td>
					</tr>
				<% end %>
		</table>
	</div>
</div>